module.exports = function(Devicesnmpconfig) {
  // Devicesnmpconfig.observe('before save',function(ctx,next){
  //   ctx.instance && (ctx.instance.id = ctx.instance.device_id)
  //   next()
  // })
};
