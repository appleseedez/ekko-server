module.exports = function(Devicechannel) {

  // Devicechannel.observe('before save',function(ctx,next){
  //   console.log(typeof ctx.instance.id);
  //   // ctx.instance && (ctx.instance.id = ctx.instance.device_id)
  //   next()
  // })
};
